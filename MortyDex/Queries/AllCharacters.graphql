query AllCharacters($page: Int) {
  characters(page: $page) {
    results {
      name
      image
      status
      species
      gender
      origin {
        name
      }
      location {
        name
      }
    }
  }
}
