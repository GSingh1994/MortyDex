query AllCharacters($page: Int) {
  characters(page: $page) {
    results {
      id
      name
      image
      status
      species
      gender
      origin {
        name
      }
      location {
        name
      }
      episode {
        id
        episode
        name
        air_date
      }
    }
  }
}
